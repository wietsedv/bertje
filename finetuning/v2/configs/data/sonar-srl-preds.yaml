data:
  name: "sonar-srl-preds"
  input: "data/sonar-srl-preds"
  num_labels: 4

train:
  max_epochs: 200
  batch_size: 16
#   attention_dropout: 0.2
#   hidden_dropout: 0.2

# optimizer:
#   warmup_steps: 256
#   learning_rate: 0.00001
