data:
  name: "sonar-srl-mods"
  input: "data/sonar-srl-mods"
  num_labels: 29

train:
  max_epochs: 200
  batch_size: 16
#   attention_dropout: 0.2
#   hidden_dropout: 0.2

# optimizer:
#   warmup_steps: 256
#   learning_rate: 0.00001
